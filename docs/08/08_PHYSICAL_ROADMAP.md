# 08 — Roadmap to Physical Substrate

```
CURRENT (software + RCF bitstream file)
    → live FPGA hot-load (A)
    → minimal robot on body bus (B)
    → physical ECR edge reopen (C)
    → MF certify new matter → package → RCF hot-load
    → persistent robot capability change
    → multi-node fabric
    → ASIC macros
```

**A19→A40 framing:** fixed physical resources; **effective organization** changes by loading **certified matter**, not by replacing host silicon.
