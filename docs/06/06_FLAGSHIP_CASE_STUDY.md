# 06 — Flagship Case Study: ECR2MF_g0_field_s1002

```
unknown world → field discovery → intervention → certificate
 → MF archive → lineage → ×211 subsequent uses
 → faithful IR → hardware representation → RTL
 → RCF generic host path → 135100-byte iCE40 bitstream
```

**Standalone:** `STANDALONE_FLAGSHIP/` runs load → cert → lineage → execute → gate-off Δ≈1.92 → hot-load/unload/restore → bitstream check.

This single object carries the thesis from discovery to hardware artifact without requiring the full research tree.
